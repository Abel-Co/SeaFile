/**
 * 迁入使用：unplugin-vue-components
 * Url：https://github.com/antfu/unplugin-vue-components.
 * Declare：vite-plugin-components has been renamed to unplugin-vue-components
 */
// import { decamelize } from '../utils/decamelize'
//
// const components = import.meta.globEager('./**/*.vue')
//
// Object.assign(components)
//
// export default {
//   install(app) {
//     Object.keys(components).forEach(path => {
//       const name = path.split(/[./]/)[2]  // decamelize(path.split(/[./]/)[2]) // 连字符方式 '-'
//       app.component(`${name}`, components[path].default)
//     })
//   }
// }
