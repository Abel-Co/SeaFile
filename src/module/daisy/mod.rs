
// fn list() -> Vec<Any> {
//     let list = vec!["",""];
//     let _ = list.sort();
//     list
// }
//
// use rayon::prelude::*;
// fn sort(a, b) -> bool {
//     if a.children { sort(a.children())}
//     if b.children {sort(b.children())}
//     files.par_sort_by(|a, b| a.cmp(&b));    // 排序
//     files
// }